# Operation Summary

## Field

- Name: `{field_name}`
- Id: `{field_id}`

## Operation

- Type: `{operation_type}` (`clone-context` or `sync-options-all-contexts`)
- Requested by user input: `{user_inputs}`

## Context Results

- Total contexts discovered: `{contexts_total}`
- Contexts processed: `{contexts_processed}`
- Contexts succeeded: `{contexts_succeeded}`
- Contexts failed: `{contexts_failed}`

## Changes Applied

- Options created: `{options_created_total}`
- Options enabled: `{options_enabled_total}`
- Options disabled: `{options_disabled_total}`
- Reorder operations: `{reorder_ops_total}`

## Details

- `{per_context_details}`

## Final Status

- `{final_status}`
- If failed/partial: `{failed_step_and_error}`
